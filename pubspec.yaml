name: shop_app
description: "A new Flutter E-commerce app enable the user to search for products and add buy it "

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  bloc: ^8.1.4
  flutter_bloc: ^8.1.6
  http: ^1.2.2
  flutter_secure_storage: ^9.2.4
  smooth_page_indicator: ^1.2.0+3
  shared_preferences: ^2.3.5
  salomon_bottom_bar: ^3.3.2
  skeletonizer: ^1.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets: